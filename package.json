{
  "name": "rusty-space",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "authoring:materialize": "pnpm --dir gameplay/authoring materialize",
    "authoring:check": "pnpm --dir gameplay/authoring check",
    "build": "pnpm authoring:check && pnpm --dir apps/web build",
    "typecheck": "pnpm --dir gameplay/authoring typecheck && pnpm --dir apps/web typecheck",
    "test:browser": "pnpm --dir apps/web test:browser",
    "verify": "pnpm authoring:check && cargo fmt --check && cargo test --workspace --locked && cargo clippy --workspace --all-targets --locked -- -D warnings && pnpm typecheck && pnpm --dir apps/web build && pnpm test:browser"
  }
}
